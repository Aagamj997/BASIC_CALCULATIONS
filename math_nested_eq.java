public class math_nested_eq{
    public static void main(){
        double x=4;
        double y=2;
        double z=Math.pow(y,2);
        double ans=Math.sqrt(x+3*y);
        System.out.print(ans);
    }
}